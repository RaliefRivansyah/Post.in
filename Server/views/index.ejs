<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <title>Home - SocialApp</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    </head>
    <body class="bg-light">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="/">Postin</a>
                <% if (token) { %>
                <div class="d-flex gap-2">
                    <a href="/posts" class="btn btn-outline-light btn-sm">Posts</a>
                    <a href="/profile" class="btn btn-outline-light btn-sm">Profile</a>
                    <a href="/logout" class="btn btn-outline-light btn-sm">Logout</a>
                </div>
                <% } %>
            </div>
        </nav>

        <div class="container">
            <div class="text-center mt-5">
                <h1 class="display-5 fw-bold">Welcome to Postin</h1>
                <p class="lead">Simple social media</p>
                <% if (!token) { %>
                <a href="/login" class="btn btn-primary">Login</a>
                <a href="/register" class="btn btn-outline-primary">Register</a>
                <% } else { %>
                <a href="/posts" class="btn btn-success">Go to Posts</a>
                <% } %>
            </div>
        </div>
    </body>
</html>
